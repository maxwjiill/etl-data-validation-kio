experiment:
  name: tools_stage_comparison
  baseline:
    stg_run_id: __STG_RUN_ID__
    dds_run_id: __DDS_RUN_ID__
  defaults:
    output_dir: logs/etl_stage_reports
    include_experiments: true
    only_unprocessed: false
    repeats: 3
    tools_by_stage:
      E:
      - gx
      - soda
      T:
      - dbt
      - gx
      - soda
      - sql
      L:
      - gx
      - soda
      - dbt
